import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  imgBG: {
    flex: 1,
    resizeMode: "cover",
  },

  container: {
    flex: 1,
    flexDirection: "column",
    padding: 15,
  },
});
